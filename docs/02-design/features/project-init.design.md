# Design: NeedCash 프로젝트 초기화

> Plan 문서 기반 상세 기술 설계서

---

## 1. 파일 구조 설계

```
NeedCash/
├── apps/
│   └── web/
│       ├── app/
│       │   ├── layout.tsx                 # RootLayout: 폰트, 테마, 메타데이터
│       │   ├── page.tsx                   # 메인 홈페이지
│       │   ├── not-found.tsx              # 404 페이지
│       │   ├── blog/
│       │   │   ├── page.tsx               # 블로그 목록
│       │   │   └── [slug]/
│       │   │       └── page.tsx           # 블로그 상세 (MDX 렌더링)
│       │   ├── game/
│       │   │   ├── page.tsx               # 게임 목록 그리드
│       │   │   └── [slug]/
│       │   │       └── page.tsx           # 게임 상세 (임베드)
│       │   ├── ads/
│       │   │   └── page.tsx               # 광고 랜딩 페이지
│       │   └── resume/
│       │       └── page.tsx               # 인터랙티브 이력서
│       │
│       ├── components/
│       │   ├── layout/
│       │   │   ├── header.tsx             # 상단 내비게이션
│       │   │   ├── footer.tsx             # 하단 푸터
│       │   │   └── nav-links.tsx          # 내비게이션 링크 목록
│       │   ├── ui/
│       │   │   ├── button.tsx             # 버튼 컴포넌트
│       │   │   ├── card.tsx               # 카드 컴포넌트
│       │   │   ├── tag.tsx                # 태그/뱃지 컴포넌트
│       │   │   └── scroll-reveal.tsx      # 스크롤 등장 래퍼 (framer-motion)
│       │   ├── theme/
│       │   │   ├── theme-provider.tsx     # next-themes Provider
│       │   │   └── theme-switcher.tsx     # 테마 전환 UI
│       │   ├── blog/
│       │   │   ├── post-card.tsx          # 블로그 포스트 카드
│       │   │   ├── post-list.tsx          # 포스트 목록
│       │   │   ├── mdx-components.tsx     # MDX 커스텀 컴포넌트
│       │   │   └── toc.tsx               # 목차 (Table of Contents)
│       │   ├── game/
│       │   │   ├── game-card.tsx          # 게임 카드
│       │   │   ├── dice-game.tsx          # 주사위 게임 컴포넌트
│       │   │   └── lotto-game.tsx         # 로또번호 뽑기 컴포넌트
│       │   └── resume/
│       │       ├── timeline.tsx           # 경력 타임라인
│       │       └── skill-chart.tsx        # 스킬 시각화
│       │
│       ├── content/
│       │   └── blog/
│       │       ├── hello-world.mdx        # 샘플 포스트 1
│       │       └── getting-started.mdx    # 샘플 포스트 2
│       │
│       ├── lib/
│       │   ├── mdx.ts                     # MDX 파싱, frontmatter 추출
│       │   ├── utils.ts                   # cn() 등 공통 유틸
│       │   └── constants.ts               # 사이트 메타 정보, 내비게이션 정의
│       │
│       ├── styles/
│       │   └── globals.css                # Tailwind 임포트 + 테마 CSS 변수
│       │
│       ├── public/
│       │   ├── fonts/                     # 로컬 폰트 파일 (선택)
│       │   └── images/                    # 정적 이미지
│       │
│       ├── next.config.ts                 # Next.js 설정
│       ├── tailwind.config.ts             # Tailwind 설정 (테마 확장)
│       ├── tsconfig.json                  # TypeScript 설정
│       ├── eslint.config.mjs              # ESLint flat config
│       ├── postcss.config.mjs             # PostCSS (Tailwind)
│       └── package.json                   # 의존성 + 스크립트
│
├── doc/                                   # PDCA 문서
├── CLAUDE.md
└── .gitignore
```

---

## 2. 의존성 설계

### package.json

```json
{
  "name": "needcash-web",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "^15",
    "react": "^19",
    "react-dom": "^19",
    "next-themes": "^0.4",
    "framer-motion": "^11",
    "next-mdx-remote": "^5",
    "shiki": "^1",
    "gray-matter": "^4",
    "clsx": "^2",
    "tailwind-merge": "^2"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^22",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "tailwindcss": "^4",
    "@tailwindcss/postcss": "^4",
    "eslint": "^9",
    "eslint-config-next": "^15",
    "@eslint/js": "^9",
    "typescript-eslint": "^8"
  }
}
```

---

## 3. 테마 시스템 설계

### 3.1 CSS 변수 기반 테마 (`globals.css`)

```css
@import "tailwindcss";

@theme {
  /* 공통 폰트 */
  --font-heading: "Plus Jakarta Sans", sans-serif;
  --font-body: "Pretendard", sans-serif;
  --font-mono: "JetBrains Mono", monospace;

  /* 공통 간격 */
  --spacing-page: clamp(1rem, 5vw, 6rem);
}

/* ── Dark 테마 (기본) ── */
:root {
  --color-bg: #0a0a0a;
  --color-bg-secondary: #141414;
  --color-bg-tertiary: #1e1e1e;
  --color-text: #fafafa;
  --color-text-secondary: #a0a0a0;
  --color-text-muted: #666666;
  --color-border: #262626;
  --color-accent: #e0e0e0;
  --color-accent-hover: #ffffff;
  --color-link: #c8c8c8;
  --color-link-hover: #ffffff;
  --color-code-bg: #1a1a1a;
  --color-card-bg: #111111;
  --color-card-border: #222222;
}

/* ── Light 테마 ── */
[data-theme="light"] {
  --color-bg: #fafafa;
  --color-bg-secondary: #f0f0f0;
  --color-bg-tertiary: #e5e5e5;
  --color-text: #0a0a0a;
  --color-text-secondary: #525252;
  --color-text-muted: #999999;
  --color-border: #e0e0e0;
  --color-accent: #171717;
  --color-accent-hover: #000000;
  --color-link: #404040;
  --color-link-hover: #000000;
  --color-code-bg: #f0f0f0;
  --color-card-bg: #ffffff;
  --color-card-border: #e5e5e5;
}
```

### 3.2 ThemeProvider 설계

```tsx
// components/theme/theme-provider.tsx
"use client";
import { ThemeProvider as NextThemesProvider } from "next-themes";

export function ThemeProvider({ children }: { children: React.ReactNode }) {
  return (
    <NextThemesProvider
      attribute="data-theme"
      defaultTheme="dark"
      themes={["dark", "light"]}
      disableTransitionOnChange={false}
    >
      {children}
    </NextThemesProvider>
  );
}
```

### 3.3 ThemeSwitcher 설계

```tsx
// components/theme/theme-switcher.tsx
"use client";
import { useTheme } from "next-themes";

// 아이콘 기반 토글: 해/달 아이콘
// 클릭 시 dark ↔ light 전환
// 마운트 전 hydration 불일치 방지를 위한 mounted 체크
```

---

## 4. 레이아웃 설계

### 4.1 RootLayout (`app/layout.tsx`)

```tsx
// 구조:
// <html lang="ko" suppressHydrationWarning>
//   <body className={`${fontBody.variable} ${fontHeading.variable} ${fontMono.variable}`}>
//     <ThemeProvider>
//       <Header />
//       <main>{children}</main>
//       <Footer />
//     </ThemeProvider>
//   </body>
// </html>

// 메타데이터:
// title: "NeedCash"
// description: "프로토타입 허브 - 게임, 블로그, 광고, 이력서"
```

### 4.2 Header 설계

```
┌──────────────────────────────────────────────────────┐
│  NeedCash              Blog  Game  Ads  Resume   ◐  │
└──────────────────────────────────────────────────────┘

모바일 (<768px):
┌──────────────────────────────────────────────────────┐
│  NeedCash                                    ☰   ◐  │
└──────────────────────────────────────────────────────┘
  ↓ 메뉴 오픈 시
┌──────────────────────────────────────────────────────┐
│  Blog                                                │
│  Game                                                │
│  Ads                                                 │
│  Resume                                              │
└──────────────────────────────────────────────────────┘
```

- 고정 상단 (`sticky top-0`)
- 배경 블러 (`backdrop-blur`)
- 스크롤 시 테두리 등장
- ◐ = ThemeSwitcher

### 4.3 Footer 설계

```
┌──────────────────────────────────────────────────────┐
│                                                      │
│  NeedCash                                            │
│  Built with Next.js                                  │
│                                                      │
│  © 2026 NeedCash. All rights reserved.               │
│                                                      │
└──────────────────────────────────────────────────────┘
```

- 미니멀 디자인, 과도한 링크 없음
- 연도 자동 계산

---

## 5. 페이지별 상세 설계

### 5.1 메인 홈페이지 (`/`)

```
┌──────────────────────────────────────────────────────┐
│  [Header]                                            │
├──────────────────────────────────────────────────────┤
│                                                      │
│                                                      │
│           NeedCash                                   │
│           프로토타입 허브                               │
│           게임, 블로그, 광고, 이력서를                    │
│           하나의 공간에서.                               │
│                                                      │
│                                                      │
├──────────────────────────────────────────────────────┤
│                                                      │
│  ┌────────────┐  ┌────────────┐                      │
│  │   Blog →   │  │   Game →   │                      │
│  │  최신 글    │  │  게임 모음  │                      │
│  └────────────┘  └────────────┘                      │
│  ┌────────────┐  ┌────────────┐                      │
│  │   Ads →    │  │  Resume →  │                      │
│  │  랜딩 페이지 │  │  이력서    │                      │
│  └────────────┘  └────────────┘                      │
│                                                      │
├──────────────────────────────────────────────────────┤
│  최신 블로그                                          │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐             │
│  │ Post 1   │ │ Post 2   │ │ Post 3   │             │
│  │ 제목...   │ │ 제목...   │ │ 제목...   │             │
│  │ 날짜      │ │ 날짜      │ │ 날짜      │             │
│  └──────────┘ └──────────┘ └──────────┘             │
│                                                      │
├──────────────────────────────────────────────────────┤
│  [Footer]                                            │
└──────────────────────────────────────────────────────┘
```

**핵심 요소:**
- 히어로: 풀 뷰포트 높이, 대담한 타이포그래피, 가운데 정렬
- 섹션 카드: 2x2 그리드, 호버 시 미세한 이동 + 색상 변화
- 최신 블로그: 가로 스크롤 또는 3열 그리드
- 모든 섹션 `ScrollReveal` 래퍼로 등장 애니메이션

### 5.2 블로그 (`/blog`)

**목록 페이지:**
```
┌──────────────────────────────────────────────────────┐
│  Blog                                                │
│                                                      │
│  [All] [Tech] [Design] [Life]     ← 카테고리 필터     │
│                                                      │
│  ┌──────────────────────────────────────────┐        │
│  │  포스트 제목                               │        │
│  │  요약 텍스트 한두 줄...                     │        │
│  │  2026-02-01 · 5 min read · #tech         │        │
│  └──────────────────────────────────────────┘        │
│  ┌──────────────────────────────────────────┐        │
│  │  포스트 제목 2                             │        │
│  │  요약 텍스트...                            │        │
│  │  2026-01-28 · 3 min read · #design       │        │
│  └──────────────────────────────────────────┘        │
└──────────────────────────────────────────────────────┘
```

**상세 페이지:**
```
┌──────────────────────────────────────────────────────┐
│  ← Back to Blog                                      │
│                                                      │
│  포스트 제목                                          │
│  2026-02-01 · 5 min read                             │
│  #tech #react                                        │
│                                                      │
│  ┌──────┐  ┌─────────────────────────────────┐       │
│  │ TOC  │  │  MDX 본문 콘텐츠                  │       │
│  │ H1   │  │  마크다운 렌더링                   │       │
│  │ H2   │  │  코드 블록 (shiki 하이라이팅)      │       │
│  │ H3   │  │  이미지, 테이블 등                 │       │
│  └──────┘  └─────────────────────────────────┘       │
└──────────────────────────────────────────────────────┘
```

**MDX Frontmatter 스키마:**
```yaml
---
title: "포스트 제목"
description: "포스트 요약"
date: "2026-02-01"
category: "tech"
tags: ["react", "nextjs"]
published: true
---
```

**MDX 파싱 로직 (`lib/mdx.ts`):**
- `fs.readdir` → content/blog/ 디렉토리 읽기
- `gray-matter` → frontmatter + 본문 분리
- `next-mdx-remote/rsc` → 서버 컴포넌트에서 MDX 렌더링
- `shiki` → 코드 블록 하이라이팅
- 읽기 시간 계산: `Math.ceil(wordCount / 200)`

### 5.3 게임 허브 (`/game`)

**목록 페이지:**
```
┌──────────────────────────────────────────────────────┐
│  Games                                               │
│                                                      │
│  ┌────────────────────┐  ┌────────────────────┐      │
│  │  🎲                │  │  🍀                │      │
│  │  Dice Roller       │  │  Lotto Pick        │      │
│  │  주사위 2개를 굴려   │  │  행운의 로또번호를    │      │
│  │  운을 시험하세요     │  │  뽑아보세요          │      │
│  │                    │  │                    │      │
│  │  [ 플레이 → ]      │  │  [ 플레이 → ]      │      │
│  └────────────────────┘  └────────────────────┘      │
└──────────────────────────────────────────────────────┘
```

**게임 데이터 구조 (`lib/constants.ts`):**
```ts
interface Game {
  slug: string;
  title: string;
  description: string;
  emoji: string;         // 카드 썸네일 대용 이모지
  type: "component";     // React 컴포넌트 기반
  component: string;     // 컴포넌트 이름 매핑
}

const GAMES: Game[] = [
  {
    slug: "dice",
    title: "Dice Roller",
    description: "주사위 2개를 굴려 운을 시험하세요",
    emoji: "🎲",
    type: "component",
    component: "DiceGame",
  },
  {
    slug: "lotto",
    title: "Lotto Pick",
    description: "행운의 로또번호를 뽑아보세요",
    emoji: "🍀",
    type: "component",
    component: "LottoGame",
  },
];
```

- 게임 데이터는 `lib/constants.ts`에 배열로 정의 (DB 없음)
- 모든 게임은 React 컴포넌트로 구현 (iframe 불필요)
- 차후 백엔드 추가 시 API 호출로 교체 가능
- 새 게임 추가 시 컴포넌트 작성 + GAMES 배열에 항목 추가

---

#### 5.3.1 주사위 게임 (`/game/dice`)

**와이어프레임:**
```
┌──────────────────────────────────────────────────────┐
│  ← Games                                             │
│                                                      │
│              🎲 Dice Roller                          │
│                                                      │
│         ┌─────────┐    ┌─────────┐                   │
│         │         │    │         │                   │
│         │  ⚃  3   │    │  ⚅  6   │                   │
│         │         │    │         │                   │
│         └─────────┘    └─────────┘                   │
│                                                      │
│              합계: 9                                  │
│                                                      │
│           [ 🎲 굴리기 ]                               │
│                                                      │
│  ─────────────────────────────────────               │
│  기록                                                 │
│  #3  ⚂⚄  3 + 5 = 8                                  │
│  #2  ⚀⚅  1 + 6 = 7                                  │
│  #1  ⚃⚃  4 + 4 = 8  🎯 더블!                        │
│                                                      │
└──────────────────────────────────────────────────────┘
```

**컴포넌트 설계 (`components/game/dice-game.tsx`):**

```ts
// State
interface DiceGameState {
  dice: [number, number];       // 각 주사위 값 (1-6)
  isRolling: boolean;           // 굴리기 애니메이션 중
  history: {
    id: number;
    dice: [number, number];
    sum: number;
    isDouble: boolean;
  }[];
}

// 주요 기능:
// 1. "굴리기" 버튼 클릭 → 랜덤 숫자 2개 생성 (1-6)
// 2. 굴리는 동안 주사위 흔들림 애니메이션 (framer-motion, ~0.6s)
// 3. 결과 표시: 각 주사위 눈 + 합계
// 4. 더블(같은 숫자) 시 특별 표시
// 5. 기록 리스트 (최근 10개, 최신순)

// 주사위 눈 렌더링:
// CSS Grid로 점 배치 (1~6 각각의 패턴)
// 다크/라이트 테마 대응 (배경: card-bg, 점: text 색상)

// 애니메이션:
// framer-motion animate + transition
// 굴리기: rotateX/Y 랜덤 회전 → 결과 값으로 정지
// 결과 숫자: scale 0→1 팝 효과
```

---

#### 5.3.2 로또번호 뽑기 게임 (`/game/lotto`)

**와이어프레임:**
```
┌──────────────────────────────────────────────────────┐
│  ← Games                                             │
│                                                      │
│              🍀 Lotto Pick                           │
│                                                      │
│    ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐       │
│    │ 03 │ │ 15 │ │ 22 │ │ 31 │ │ 38 │ │ 44 │       │
│    └────┘ └────┘ └────┘ └────┘ └────┘ └────┘       │
│                              + 보너스 ┌────┐         │
│                                       │ 07 │         │
│                                       └────┘         │
│                                                      │
│           [ 🍀 번호 뽑기 ]                            │
│                                                      │
│  ─────────────────────────────────────               │
│  이전 결과                                            │
│  #3  03 15 22 31 38 44  + 07                         │
│  #2  01 09 17 25 33 41  + 12                         │
│  #1  05 11 19 28 36 45  + 02                         │
│                                                      │
└──────────────────────────────────────────────────────┘
```

**컴포넌트 설계 (`components/game/lotto-game.tsx`):**

```ts
// State
interface LottoGameState {
  numbers: number[];            // 당첨번호 6개 (1-45, 오름차순)
  bonus: number;                // 보너스 번호 1개
  isDrawing: boolean;           // 추첨 애니메이션 중
  history: {
    id: number;
    numbers: number[];
    bonus: number;
  }[];
}

// 로또 규칙 (한국 로또 6/45 기준):
// 1. 1~45 중 중복 없이 6개 추첨
// 2. 6개를 오름차순 정렬
// 3. 나머지 중 1개를 보너스 번호로 추첨

// 번호 공 색상 (한국 로또 공식 색상):
// 1-10:  노란색 (#fbc400)
// 11-20: 파란색 (#69c8f2)
// 21-30: 빨간색 (#ff7272)
// 31-40: 회색   (#aaaaaa)
// 41-45: 초록색 (#b0d840)

// 주요 기능:
// 1. "번호 뽑기" 클릭 → 랜덤 6+1개 생성
// 2. 추첨 애니메이션: 공이 하나씩 순서대로 등장 (stagger 0.3s)
// 3. 각 공은 숫자 범위에 따른 색상 적용
// 4. 보너스 번호는 "+" 구분자 후 별도 표시
// 5. 기록 리스트 (최근 10개, 최신순)

// 공 컴포넌트:
// 원형(rounded-full), 너비 48px
// 배경색: 번호 범위별 색상
// 텍스트: 흰색 볼드, 2자리 표시 (01, 02, ... 45)
// 등장 애니메이션: scale 0→1 + 위에서 떨어지는 효과

// 추첨 시퀀스:
// 1번공(0.0s) → 2번공(0.3s) → ... → 6번공(1.5s) → 보너스(2.0s)
```

**공 색상 매핑 유틸:**
```ts
function getBallColor(num: number): string {
  if (num <= 10) return "#fbc400"; // 노란색
  if (num <= 20) return "#69c8f2"; // 파란색
  if (num <= 30) return "#ff7272"; // 빨간색
  if (num <= 40) return "#aaaaaa"; // 회색
  return "#b0d840";                // 초록색
}
```

---

#### 5.3.3 게임 상세 페이지 (`/game/[slug]`)

```tsx
// app/game/[slug]/page.tsx
// slug에 따라 해당 게임 컴포넌트를 렌더링

// 컴포넌트 매핑:
const GAME_COMPONENTS: Record<string, React.ComponentType> = {
  dice: DiceGame,
  lotto: LottoGame,
};

// generateStaticParams로 ["dice", "lotto"] 사전 생성
// 존재하지 않는 slug → notFound()
```

### 5.4 광고 랜딩 페이지 (`/ads`)

```
┌──────────────────────────────────────────────────────┐
│  히어로 섹션                                          │
│  대담한 헤드라인                                       │
│  부제목 설명 텍스트                                    │
│  [ CTA 버튼 ]                                        │
│                                                      │
├──────────────────────────────────────────────────────┤
│  특징 1       특징 2       특징 3                      │
│  설명         설명         설명                        │
├──────────────────────────────────────────────────────┤
│  사회적 증거 / 추천사                                  │
├──────────────────────────────────────────────────────┤
│  최종 CTA 섹션                                        │
│  [ 지금 시작하기 ]                                    │
└──────────────────────────────────────────────────────┘
```

- 단일 페이지 구조, 스크롤 내비게이션
- 템플릿 형태로 콘텐츠 교체 가능하게 설계

### 5.5 이력서 (`/resume`)

```
┌──────────────────────────────────────────────────────┐
│  이름                                                 │
│  직함 / 한 줄 소개                                    │
│  [ PDF 다운로드 ]                                     │
│                                                      │
├──────────────────────────────────────────────────────┤
│  경력                                                 │
│  ──●── 2024-현재: 회사/프로젝트                        │
│    │   설명...                                        │
│  ──●── 2022-2024: 회사/프로젝트                        │
│    │   설명...                                        │
│  ──●── 2020-2022: 회사/프로젝트                        │
│                                                      │
├──────────────────────────────────────────────────────┤
│  Skills                                               │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐                │
│  │ React │ │ Next │ │ TS   │ │ Node │                │
│  │ ████░ │ │ ████░│ │ ███░░│ │ ███░░│                │
│  └──────┘ └──────┘ └──────┘ └──────┘                │
├──────────────────────────────────────────────────────┤
│  교육, 프로젝트, 연락처                                │
└──────────────────────────────────────────────────────┘
```

**이력서 데이터 구조:**
```ts
interface ResumeData {
  name: string;
  title: string;
  bio: string;
  experience: {
    company: string;
    role: string;
    period: string;
    description: string;
  }[];
  skills: {
    name: string;
    level: number;   // 1-5
    category: string; // "Frontend" | "Backend" | "Tool"
  }[];
  education: {
    school: string;
    degree: string;
    period: string;
  }[];
  contact: {
    email?: string;
    github?: string;
    linkedin?: string;
  };
}
```

- 데이터는 `lib/constants.ts`에 정의
- PDF 다운로드: `window.print()` 기반 또는 차후 라이브러리 추가

---

## 6. 컴포넌트 설계

### 6.1 ScrollReveal (스크롤 등장 애니메이션)

```tsx
// components/ui/scroll-reveal.tsx
"use client";
import { motion } from "framer-motion";

// Props: children, delay?, direction? ("up" | "left" | "right")
// framer-motion의 whileInView + viewport once 활용
// 기본 동작: opacity 0→1, translateY 20px→0
// 딜레이 조절로 카드 순차 등장 효과
```

### 6.2 Card

```tsx
// components/ui/card.tsx
// Props: children, href?, className?
// 호버: 테두리 밝아짐 + 미세한 translateY(-2px)
// 클릭 가능 시 cursor-pointer + 전체 카드 링크
```

### 6.3 Button

```tsx
// components/ui/button.tsx
// variants: "default" | "outline" | "ghost"
// sizes: "sm" | "md" | "lg"
// Tailwind 기반 스타일링, cn() 유틸 활용
```

### 6.4 Tag

```tsx
// components/ui/tag.tsx
// Props: label, active?, onClick?
// 블로그 카테고리/태그 필터에 사용
// 활성 상태 시 accent 색상 배경
```

---

## 7. 라우팅 설계

| 경로 | 파일 | 렌더링 | 설명 |
|------|------|--------|------|
| `/` | `app/page.tsx` | SSG | 메인 홈페이지 |
| `/blog` | `app/blog/page.tsx` | SSG | 블로그 목록 |
| `/blog/[slug]` | `app/blog/[slug]/page.tsx` | SSG | 블로그 상세 |
| `/game` | `app/game/page.tsx` | SSG | 게임 목록 |
| `/game/[slug]` | `app/game/[slug]/page.tsx` | SSG | 게임 상세 |
| `/ads` | `app/ads/page.tsx` | SSG | 광고 랜딩 |
| `/resume` | `app/resume/page.tsx` | SSG | 이력서 |

- 모든 페이지 SSG (정적 생성) - Backend 없으므로 최적 성능
- `generateStaticParams`로 블로그/게임 동적 경로 사전 생성

---

## 8. 반응형 설계

### 브레이크포인트

| 이름 | 너비 | 레이아웃 |
|------|------|----------|
| mobile | < 640px | 단일 컬럼, 햄버거 메뉴 |
| tablet | 640px - 1024px | 2열 그리드 |
| desktop | > 1024px | 다열 그리드, 사이드바 TOC |

### 주요 반응형 패턴

- **Header**: 데스크톱=가로 링크, 모바일=햄버거 메뉴
- **홈 섹션 카드**: 데스크톱=2x2, 태블릿=2x1, 모바일=1열
- **블로그 목록**: 데스크톱=단일 리스트, 모바일=컴팩트 카드
- **블로그 상세**: 데스크톱=사이드 TOC, 모바일=상단 접이식 TOC
- **게임 그리드**: 데스크톱=3열, 태블릿=2열, 모바일=1열
- **이력서**: 데스크톱=2열(타임라인+스킬), 모바일=1열 스택

---

## 9. 애니메이션 설계

| 요소 | 트리거 | 동작 |
|------|--------|------|
| 히어로 텍스트 | 페이지 로드 | 아래에서 위로 등장 (stagger 0.1s) |
| 섹션 카드 | 스크롤 진입 | 아래에서 위로 등장 (stagger 0.05s) |
| 블로그 카드 | 스크롤 진입 | 페이드인 + 위로 슬라이드 |
| 카드 호버 | 마우스 오버 | translateY(-2px) + 테두리 밝아짐 |
| 페이지 전환 | 라우트 변경 | opacity 전환 (CSS transition) |
| 테마 전환 | 토글 클릭 | CSS transition 0.3s (배경, 텍스트 색상) |
| 모바일 메뉴 | 햄버거 클릭 | 위에서 아래로 슬라이드 |
| 주사위 굴리기 | 버튼 클릭 | rotateX/Y 랜덤 회전 0.6s → 결과 정지 |
| 주사위 결과 | 굴리기 완료 | scale 0→1 팝 효과 |
| 로또 공 등장 | 번호 뽑기 클릭 | 순차 등장 stagger 0.3s, 위에서 아래로 + scale |
| 로또 보너스 | 6번째 공 후 | 0.5s 딜레이 후 특별 등장 (bounce) |

---

## 10. 구현 순서

### Step 1: 프로젝트 초기화
1. `apps/web/` 디렉토리에 Next.js 15 프로젝트 생성
2. Tailwind CSS 4, ESLint, TypeScript 설정
3. `globals.css`에 테마 CSS 변수 정의
4. `lib/utils.ts` 생성 (cn 유틸)
5. `lib/constants.ts` 생성 (사이트 메타, 내비게이션)

### Step 2: 레이아웃 + 테마
1. `ThemeProvider`, `ThemeSwitcher` 구현
2. `Header` 구현 (반응형 + 모바일 메뉴)
3. `Footer` 구현
4. `RootLayout`에 조립
5. 폰트 설정 (next/font)

### Step 3: UI 컴포넌트
1. `Button`, `Card`, `Tag` 구현
2. `ScrollReveal` 구현

### Step 4: 메인 홈페이지
1. 히어로 섹션 (대담한 타이포)
2. 섹션 네비게이션 카드 (2x2 그리드)
3. 최신 블로그 프리뷰 (MDX 연동 후)

### Step 5: 블로그
1. `lib/mdx.ts` MDX 파싱 유틸 구현
2. MDX 커스텀 컴포넌트 (코드 블록, 이미지 등)
3. 블로그 목록 페이지
4. 블로그 상세 페이지 + TOC
5. 샘플 MDX 포스트 작성

### Step 6: 이력서
1. 이력서 데이터 정의
2. 타임라인 컴포넌트
3. 스킬 차트 컴포넌트
4. PDF 다운로드 기능

### Step 7: 게임 허브
1. 게임 데이터 정의 (`GAMES` 배열 in `lib/constants.ts`)
2. 게임 목록 페이지 + 게임 카드 그리드
3. 게임 상세 페이지 라우팅 (`/game/[slug]` + 컴포넌트 매핑)
4. 주사위 게임 구현 (`dice-game.tsx`): 주사위 눈 CSS + 굴리기 애니메이션 + 기록
5. 로또 게임 구현 (`lotto-game.tsx`): 번호 공 색상 + 순차 등장 애니메이션 + 기록

### Step 8: 광고 랜딩
1. 랜딩 페이지 템플릿 구현
2. CTA 섹션

### Step 9: 마무리
1. 404 페이지
2. 메타데이터 (OG tags, 파비콘)
3. 성능 점검 (Lighthouse)

---

## 11. 차후 확장 포인트

Backend 추가 시 변경이 필요한 지점:

| 현재 | 차후 | 변경 파일 |
|------|------|-----------|
| MDX 파일 → 블로그 | DB → 블로그 | `lib/mdx.ts` → `lib/api/blog.ts` |
| `constants.ts` → 게임 | DB → 게임 | `lib/constants.ts` → `lib/api/game.ts` |
| `constants.ts` → 이력서 | CMS → 이력서 | `lib/constants.ts` → `lib/api/resume.ts` |
| `window.print()` → PDF | 서버 PDF 생성 | `resume/page.tsx` |
| 없음 → 인증 | NextAuth.js | `app/api/auth/`, middleware |

데이터 소스만 교체하면 UI 코드는 재사용 가능하도록 컴포넌트와 데이터 로직을 분리한다.
